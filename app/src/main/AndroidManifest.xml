<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- For Android 10+ background geofence -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- Required for notification permission dialog on Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name=".AttendifyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Attendify">

        <receiver
            android:name=".geofence.GeofenceReceiver"
            android:exported="false"/>

        <activity
            android:name=".RoleSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.MonthDetailActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.StudentMonthDetailActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.SecretaryMonthDetailActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.DateStudentListActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.NotificationActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.ApprovalRequestsActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.ExcuseLetterActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.SubmitExcuseLetterActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.TeacherContactParentActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.SecretaryClassListActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.ExcuseLetterStatusActivity"
            android:exported="false"
            android:configChanges="uiMode" />

        <activity
            android:name=".activities.TeacherClassListActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.attendify.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>