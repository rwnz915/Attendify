<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <!-- Row background — highlight set programmatically -->
    <View
        android:id="@+id/row_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_setup_card" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <!-- Color swatches -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:id="@+id/swatch_1"
                android:layout_width="24dp"
                android:layout_height="36dp"
                android:layout_marginEnd="3dp"
                android:background="@drawable/bg_setup_icon_blue_light" />

            <View
                android:id="@+id/swatch_2"
                android:layout_width="24dp"
                android:layout_height="36dp"
                android:layout_marginEnd="3dp"
                android:background="@drawable/bg_setup_icon_green_light" />

            <View
                android:id="@+id/swatch_3"
                android:layout_width="24dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_setup_icon_purple_light" />
        </LinearLayout>

        <!-- Labels -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="14dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_theme_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Theme Name"
                android:textColor="@color/gray_800"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_theme_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Subtitle"
                android:textColor="@color/gray_500"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Check icon -->
        <ImageView
            android:id="@+id/iv_check"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/circle_check_big"
            android:visibility="invisible" />

    </LinearLayout>

</FrameLayout>